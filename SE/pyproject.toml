[tool.poetry]
name = "se"
version = "0.1.0"
description = ""
authors = ["y-maeda"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
matplotlib = "^3.8.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
